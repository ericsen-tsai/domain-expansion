services:
  postgres:
    image: postgres:16
    container_name: domain-expansion-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: domain_expansion
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

